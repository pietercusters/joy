---
phase: 13-project-workflow-settings-docs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "README has a Prerequisites section before Installation"
    - "Prerequisites documents iTerm2 Python API enablement steps"
    - "Prerequisites documents iTerm2 shell integration installation"
    - "Prerequisites documents gh CLI installation and auth"
    - "Prerequisites documents glab CLI installation and auth (for GitLab)"
  artifacts:
    - path: "README.md"
      provides: "Prerequisites section"
      contains: "## Prerequisites"
  key_links: []
---

<objective>
Add a "Prerequisites" section to the README documenting all external tool requirements for joy's v1.1 features (iTerm2 Python API, shell integration, gh CLI, glab CLI).

Purpose: Users who install joy can find all prerequisite setup steps in one place (DOC-01, D-13).
Output: Updated README.md with Prerequisites section before Installation.
</objective>

<execution_context>
@/Users/pieter/Github/joy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieter/Github/joy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-project-workflow-settings-docs/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Prerequisites section to README</name>
  <files>README.md</files>
  <read_first>
    - README.md (entire file -- need current structure to insert section correctly)
  </read_first>
  <action>
Insert a `## Prerequisites` section in README.md immediately BEFORE the existing `## Installation` section. The section must document these items in this exact order (per D-13):

```markdown
## Prerequisites

joy requires these tools for full functionality. Core features (project list, object management) work without them, but worktree discovery, MR status, and terminal integration need:

### 1. iTerm2 with Python API enabled

Open iTerm2 and enable the Python API:

**Preferences -> General -> Magic -> Enable Python API**

This is required for the terminal pane to display active sessions.

### 2. iTerm2 shell integration

Install shell integration for your shell:

```bash
curl -L https://iterm2.com/shell_integration/zsh -o ~/.iterm2_shell_integration.zsh && source ~/.iterm2_shell_integration.zsh
```

Add to your `~/.zshrc` to persist:

```bash
source ~/.iterm2_shell_integration.zsh
```

### 3. GitHub CLI (`gh`)

Required for MR/CI status on GitHub repositories.

```bash
brew install gh
gh auth login
```

### 4. GitLab CLI (`glab`) -- for GitLab repos

Required for MR/CI status on GitLab repositories. Skip if you only use GitHub.

```bash
brew install glab
glab auth login
```
```

Do NOT modify any other section of the README. Keep all existing content intact.
  </action>
  <verify>
    <automated>cd /Users/pieter/Github/joy && grep -n "## Prerequisites" README.md && grep -n "## Installation" README.md && grep -c "Python API" README.md && grep -c "shell_integration" README.md && grep -c "gh auth login" README.md && grep -c "glab auth login" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Prerequisites` section
    - `## Prerequisites` appears BEFORE `## Installation` (lower line number)
    - README.md contains `Enable Python API`
    - README.md contains `iterm2_shell_integration`
    - README.md contains `gh auth login`
    - README.md contains `glab auth login`
    - README.md still contains `## Installation` section (not deleted)
    - README.md still contains `## Usage` section (not deleted)
  </acceptance_criteria>
  <done>README has Prerequisites section documenting all 4 external tool requirements in correct order</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- documentation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | n/a | README.md | accept | Documentation file, no executable code. |
</threat_model>

<verification>
- `grep "## Prerequisites" README.md` returns a match
- Prerequisites section appears before Installation section
- All four items documented
</verification>

<success_criteria>
- README contains Prerequisites section with all 4 items per D-13
- Section is positioned before Installation
- Existing README content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/13-project-workflow-settings-docs/13-04-SUMMARY.md`
</output>
