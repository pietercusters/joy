---
phase: 11-mr-ci-status
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified: []
autonomous: false
requirements:
  - WKTR-07
  - WKTR-08
  - WKTR-09

must_haves:
  truths:
    - "User visually confirms MR badges appear correctly on worktree rows with open MRs"
    - "User visually confirms rows without MRs still show path on line 2"
    - "User visually confirms CI icons have correct colors (green/red/yellow)"
---

<objective>
Visual verification of Phase 11 MR & CI Status rendering in the live TUI.

Purpose: Automated tests verify data flow and text content, but visual layout, icon rendering, and color styling can only be confirmed by a human looking at the actual terminal. This checkpoint ensures the MR badges, CI icons, and line 2 content switching look correct in context.

Output: User approval or issue list for fixes.
</objective>

<execution_context>
@/Users/pieter/Github/joy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieter/Github/joy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-mr-ci-status/11-CONTEXT.md
@.planning/phases/11-mr-ci-status/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of MR/CI status rendering</name>
  <files></files>
  <action>
Present the user with verification instructions for the MR/CI status rendering.

What was built:
MR/PR status and CI pipeline indicators on worktree rows in the bottom-right pane. When a branch has an open MR/PR:
- Line 1 shows: branch icon, branch name, !N (MR number), open/draft icon, CI pass/fail/pending icon, then dirty/no-upstream indicators
- Line 2 shows: @author  commit-hash commit-message (instead of the file path)
When no MR exists, the row is unchanged from Phase 9 (path on line 2).

How to verify:
1. Ensure you have at least one registered repo in ~/.joy/config.toml with forge = "github" that has branches with open PRs.

2. Run: uv run joy

3. Look at the bottom-right "Worktrees" pane. For each worktree row:

   With open MR (expected):
   - Line 1: branch icon + branch name + "!42" (dimmed) + PR icon (green) or draft icon (dim) + CI icon (green check / red X / yellow dot) + dirty/upstream indicators
   - Line 2: "@username  abc1234 commit message" (dimmed)

   With draft MR (expected):
   - PR icon should be dimmed (not green)
   - Line 2 still shows author + commit

   Without MR (expected):
   - Line 1: branch icon + branch name + dirty/upstream only (unchanged from Phase 9)
   - Line 2: ~/abbreviated/path (unchanged from Phase 9)

4. If no GitHub repos are registered, temporarily add one to config.toml:
   [[repos]]
   name = "joy"
   local_path = "/Users/pieter/Github/joy"
   remote_url = "https://github.com/your-user/your-repo"
   forge = "github"

5. Press r to trigger a refresh. Verify the border_title timestamp updates and MR data refreshes.

6. Check that the icons are Nerd Font glyphs (not boxes or question marks). If icons show as placeholder boxes, the terminal font may not include Nerd Font glyphs.

7. Verify the overall layout is readable -- MR badges don't make line 1 too crowded, line 2 content is properly dimmed.

Resume signal: Type "approved" if the MR/CI rendering looks correct, or describe any visual issues to fix.
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User has visually verified MR/CI status rendering in the live TUI and approved the layout, icons, and colors.</done>
</task>

</tasks>

<verification>
User visual confirmation that:
- MR badges render correctly with expected icons and colors
- Line 2 content switches between MR context and path appropriately
- Overall layout is clean and readable
- Nerd Font icons display correctly
</verification>

<success_criteria>
User approves the visual rendering of MR/CI status in the worktree pane.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mr-ci-status/11-03-SUMMARY.md`
</output>
