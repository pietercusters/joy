---
phase: 09-worktree-pane
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified: []
autonomous: false
requirements:
  - WKTR-02
  - WKTR-03
  - WKTR-10
must_haves:
  truths:
    - "User visually confirms worktree grouping, indicators, and empty states render correctly"
    - "Nerd Font glyphs display as icons, not tofu boxes"
    - "Focus border accent appears when Tab reaches worktree pane"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the worktree pane implementation. Automated tests confirm logic correctness; this checkpoint confirms visual rendering in a real terminal with Nerd Font support.

Purpose: Catch rendering issues that headless tests cannot detect (glyph rendering, color accuracy, layout alignment).
Output: User approval or issue list for fix-up.
</objective>

<execution_context>
@/Users/pieter/Github/joy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieter/Github/joy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-worktree-pane/09-CONTEXT.md
@.planning/phases/09-worktree-pane/09-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of worktree pane rendering</name>
  <files></files>
  <action>
Present the user with verification steps for visual inspection of the worktree pane.

What was built: Full worktree pane with grouped rows, status indicators, empty states, and app-level data loading. All 17+ automated tests pass.

How to verify:
1. Ensure you have at least one repo registered in `~/.joy/repos.toml` with active worktrees (non-main/testing branches)
2. Launch joy: `uv run joy`
3. Verify the bottom-right pane (titled "Worktrees") shows:
   - Repo names as bold, muted section headers
   - Two-line worktree rows: branch name with branch icon on line 1, abbreviated path on line 2
   - Dirty indicator (filled circle icon, colored) on any worktree with uncommitted changes
   - No-upstream indicator (cloud-off icon, dim) on any worktree without a remote tracking branch
   - Clean worktrees show NO indicators after the branch name
4. Verify grouping: repos appear in alphabetical order; worktrees within each repo are alphabetical by branch
5. Press Tab until the Worktrees pane border turns accent-colored (focus indicator)
6. Verify pane is read-only: j/k/Enter do nothing in the worktree pane
7. To test empty states: temporarily rename `~/.joy/repos.toml` and relaunch -- should show "No repos registered. Add one via settings."
8. Restore `repos.toml`, then edit it to point to a repo with only main/testing branches -- should show "No active worktrees. (filtered: main, testing)"

Resume signal: Type "approved" or describe any visual/behavioral issues.
  </action>
  <verify>User confirms visual rendering is correct</verify>
  <done>User has approved the visual rendering of the worktree pane, or issues have been identified and addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan; no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No threats -- read-only verification |
</threat_model>

<verification>
User visual confirmation of all rendering behaviors listed in verification steps.
</verification>

<success_criteria>
- User approves visual rendering of worktree pane
- Nerd Font glyphs render correctly (not tofu)
- Focus border accent works on Tab
- Empty states display correct messages
</success_criteria>

<output>
After completion, create `.planning/phases/09-worktree-pane/09-03-SUMMARY.md`
</output>
