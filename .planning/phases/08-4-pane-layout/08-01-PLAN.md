---
phase: 08-4-pane-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/joy/widgets/terminal_pane.py
  - src/joy/widgets/worktree_pane.py
  - src/joy/widgets/__init__.py
  - tests/test_pane_layout.py
autonomous: true
requirements:
  - PANE-01
must_haves:
  truths:
    - "TerminalPane is a focusable widget with border_title 'Terminal' and centered muted 'coming soon' text"
    - "WorktreePane is a focusable widget with border_title 'Worktrees' and centered muted 'coming soon' text"
    - "Both stub panes have no interactive keybindings (empty BINDINGS)"
    - "Failing tests exist that describe the 4-pane grid layout and Tab focus cycling behavior"
  artifacts:
    - path: "src/joy/widgets/terminal_pane.py"
      provides: "TerminalPane stub widget"
      contains: "class TerminalPane"
    - path: "src/joy/widgets/worktree_pane.py"
      provides: "WorktreePane stub widget"
      contains: "class WorktreePane"
    - path: "src/joy/widgets/__init__.py"
      provides: "Widget exports including new panes"
      contains: "TerminalPane"
    - path: "tests/test_pane_layout.py"
      provides: "Failing tests for 4-pane layout (RED phase of TDD)"
      contains: "test_four_panes_in_grid"
  key_links:
    - from: "src/joy/widgets/__init__.py"
      to: "src/joy/widgets/terminal_pane.py"
      via: "import"
      pattern: "from joy.widgets.terminal_pane import TerminalPane"
    - from: "src/joy/widgets/__init__.py"
      to: "src/joy/widgets/worktree_pane.py"
      via: "import"
      pattern: "from joy.widgets.worktree_pane import WorktreePane"
---

<objective>
Create the two stub pane widgets (TerminalPane and WorktreePane) and write failing tests that define the 4-pane grid layout and Tab focus cycling contract.

Purpose: Establish the widget contracts and test expectations BEFORE wiring the grid in app.py. This is the RED phase — tests describe the target behavior; Plan 02 makes them pass.
Output: Two new widget files, updated __init__.py exports, and a test file with failing tests.
</objective>

<execution_context>
@/Users/pieter/Github/joy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieter/Github/joy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-4-pane-layout/08-CONTEXT.md

@src/joy/widgets/project_list.py
@src/joy/widgets/project_detail.py
@src/joy/widgets/__init__.py
@tests/test_tui.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TerminalPane and WorktreePane stub widgets</name>
  <files>src/joy/widgets/terminal_pane.py, src/joy/widgets/worktree_pane.py, src/joy/widgets/__init__.py</files>
  <read_first>
    - src/joy/widgets/project_detail.py (pattern for Widget subclass with can_focus, DEFAULT_CSS, compose)
    - src/joy/widgets/project_list.py (pattern for Widget subclass)
    - src/joy/widgets/__init__.py (current exports)
  </read_first>
  <behavior>
    - TerminalPane is a Widget subclass with can_focus=True
    - TerminalPane has border_title set to "Terminal"
    - TerminalPane has an id of "terminal-pane" (set in __init__ via super().__init__(id="terminal-pane"))
    - TerminalPane.compose() yields a single Static("coming soon") widget
    - TerminalPane has BINDINGS = [] (no interactive keys)
    - TerminalPane has DEFAULT_CSS that centers and dims the "coming soon" text
    - WorktreePane mirrors TerminalPane but with border_title "Worktrees" and id "worktrees-pane"
    - Both are importable from joy.widgets
  </behavior>
  <action>
Create `src/joy/widgets/terminal_pane.py`:

```python
"""Bottom-left pane: terminal session placeholder (Phase 12 will fill this in)."""
from __future__ import annotations

from textual.app import ComposeResult
from textual.widget import Widget
from textual.widgets import Static


class TerminalPane(Widget, can_focus=True):
    """Stub pane for terminal sessions. Focusable but no interactive keys (D-08, D-10)."""

    BINDINGS = []

    DEFAULT_CSS = """
    TerminalPane {
        height: 1fr;
        border: solid $surface-lighten-2;
    }
    TerminalPane:focus-within {
        border: solid $accent;
    }
    TerminalPane:focus {
        border: solid $accent;
    }
    TerminalPane Static {
        width: 1fr;
        height: 1fr;
        content-align: center middle;
        color: $text-muted;
        text-style: dim;
    }
    """

    def __init__(self, **kwargs) -> None:
        kwargs.setdefault("id", "terminal-pane")
        super().__init__(**kwargs)
        self.border_title = "Terminal"

    def compose(self) -> ComposeResult:
        yield Static("coming soon")
```

Create `src/joy/widgets/worktree_pane.py`:

```python
"""Bottom-right pane: worktree list placeholder (Phase 9 will fill this in)."""
from __future__ import annotations

from textual.app import ComposeResult
from textual.widget import Widget
from textual.widgets import Static


class WorktreePane(Widget, can_focus=True):
    """Stub pane for worktree list. Focusable but no interactive keys (D-08, D-10)."""

    BINDINGS = []

    DEFAULT_CSS = """
    WorktreePane {
        height: 1fr;
        border: solid $surface-lighten-2;
    }
    WorktreePane:focus-within {
        border: solid $accent;
    }
    WorktreePane:focus {
        border: solid $accent;
    }
    WorktreePane Static {
        width: 1fr;
        height: 1fr;
        content-align: center middle;
        color: $text-muted;
        text-style: dim;
    }
    """

    def __init__(self, **kwargs) -> None:
        kwargs.setdefault("id", "worktrees-pane")
        super().__init__(**kwargs)
        self.border_title = "Worktrees"

    def compose(self) -> ComposeResult:
        yield Static("coming soon")
```

Update `src/joy/widgets/__init__.py` to export both new widgets:

```python
"""Widgets subpackage for joy TUI."""
from joy.widgets.terminal_pane import TerminalPane
from joy.widgets.worktree_pane import WorktreePane

__all__ = ["TerminalPane", "WorktreePane"]
```

Per D-08 (stub widget classes in src/joy/widgets/), D-09 (border_title + centered muted "coming soon"), D-10 (focusable, no interactive keys), D-11/D-12 (uniform border, accent on focus).
  </action>
  <verify>
    <automated>cd /Users/pieter/Github/joy && uv run python -c "from joy.widgets import TerminalPane, WorktreePane; t = TerminalPane(); w = WorktreePane(); assert t.border_title == 'Terminal'; assert w.border_title == 'Worktrees'; assert t.id == 'terminal-pane'; assert w.id == 'worktrees-pane'; assert t.can_focus == True; assert w.can_focus == True; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class TerminalPane" src/joy/widgets/terminal_pane.py` returns a match
    - `grep "class WorktreePane" src/joy/widgets/worktree_pane.py` returns a match
    - `grep "can_focus=True" src/joy/widgets/terminal_pane.py` returns a match
    - `grep "can_focus=True" src/joy/widgets/worktree_pane.py` returns a match
    - `grep "border_title" src/joy/widgets/terminal_pane.py` returns a match containing "Terminal"
    - `grep "border_title" src/joy/widgets/worktree_pane.py` returns a match containing "Worktrees"
    - `grep "coming soon" src/joy/widgets/terminal_pane.py` returns a match
    - `grep "coming soon" src/joy/widgets/worktree_pane.py` returns a match
    - `grep "BINDINGS = \[\]" src/joy/widgets/terminal_pane.py` returns a match
    - `grep "BINDINGS = \[\]" src/joy/widgets/worktree_pane.py` returns a match
    - `grep "TerminalPane" src/joy/widgets/__init__.py` returns a match
    - `grep "WorktreePane" src/joy/widgets/__init__.py` returns a match
    - `uv run python -c "from joy.widgets import TerminalPane, WorktreePane"` exits 0
  </acceptance_criteria>
  <done>TerminalPane and WorktreePane exist as focusable stub widgets with border titles, centered muted "coming soon" text, empty BINDINGS, and focus-accent CSS. Both are importable from joy.widgets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests for 4-pane grid layout and Tab focus cycling</name>
  <files>tests/test_pane_layout.py</files>
  <read_first>
    - tests/test_tui.py (existing Textual pilot test patterns: mock_store fixture, app.run_test(), pilot.pause(), query_one)
    - tests/conftest.py (shared fixtures)
    - src/joy/app.py (current compose method — tests will expect Grid instead of Horizontal)
  </read_first>
  <behavior>
    - test_four_panes_in_grid: App DOM contains #projects-pane (or #project-list), #detail-pane (or #project-detail), #terminal-pane, #worktrees-pane — all four queryable
    - test_tab_cycles_four_panes: Starting from projects, pressing Tab 3 times visits detail, terminal, worktrees panes in order
    - test_tab_wraps_around: Pressing Tab from the last pane returns focus to the first pane
    - test_shift_tab_reverses: Shift+Tab from projects pane goes to worktrees pane
    - test_sub_title_updates_per_pane: Focus on each pane sets sub_title to the corresponding name ("Projects", "Detail", "Terminal", "Worktrees")
    - test_existing_project_list_works: Project list navigation (down arrow, highlight change) still works in Grid layout
    - test_existing_detail_pane_works: Enter on project still shifts focus to detail, Escape returns to list
  </behavior>
  <action>
Create `tests/test_pane_layout.py` with the following tests. These tests will FAIL initially because app.py still uses `Horizontal` with only two panes. Plan 02 will make them pass.

```python
"""Tests for Phase 8: 4-pane grid layout and Tab focus cycling (PANE-01, PANE-02)."""
from __future__ import annotations

import pytest
from unittest.mock import patch

from joy.app import JoyApp
from joy.models import Config, ObjectItem, PresetKind, Project


def _sample_projects() -> list[Project]:
    """Minimal project data for layout tests."""
    return [
        Project(
            name="project-alpha",
            objects=[
                ObjectItem(kind=PresetKind.BRANCH, value="main"),
                ObjectItem(kind=PresetKind.MR, value="https://example.com/mr/1"),
            ],
        ),
        Project(name="project-beta", objects=[]),
    ]


@pytest.fixture
def mock_store():
    """Mock store to avoid filesystem access."""
    with patch("joy.store.load_projects", return_value=_sample_projects()), \
         patch("joy.store.load_config", return_value=Config()):
        yield


# ---------------------------------------------------------------------------
# PANE-01: 4-pane grid layout
# ---------------------------------------------------------------------------


@pytest.mark.asyncio
async def test_four_panes_in_grid(mock_store):
    """PANE-01: App shows four panes — projects (TL), detail (TR), terminal (BL), worktrees (BR)."""
    app = JoyApp()
    async with app.run_test() as pilot:
        await pilot.pause(0.2)
        await app.workers.wait_for_complete()
        # All four panes must be queryable in the DOM
        assert app.query_one("#project-list") is not None
        assert app.query_one("#project-detail") is not None
        assert app.query_one("#terminal-pane") is not None
        assert app.query_one("#worktrees-pane") is not None


@pytest.mark.asyncio
async def test_grid_container_used(mock_store):
    """PANE-01/D-01: App uses a Grid container (not Horizontal)."""
    from textual.containers import Grid
    app = JoyApp()
    async with app.run_test() as pilot:
        await pilot.pause(0.1)
        grids = app.query(Grid)
        assert len(grids) >= 1, "App must contain at least one Grid container"


@pytest.mark.asyncio
async def test_stub_panes_show_coming_soon(mock_store):
    """PANE-01/D-09: Stub panes display centered 'coming soon' text."""
    app = JoyApp()
    async with app.run_test() as pilot:
        await pilot.pause(0.1)
        terminal = app.query_one("#terminal-pane")
        worktrees = app.query_one("#worktrees-pane")
        # Each should have a Static child with "coming soon"
        from textual.widgets import Static
        terminal_static = terminal.query_one(Static)
        worktrees_static = worktrees.query_one(Static)
        assert "coming soon" in str(terminal_static.renderable).lower()
        assert "coming soon" in str(worktrees_static.renderable).lower()


# ---------------------------------------------------------------------------
# PANE-02: Tab focus cycling
# ---------------------------------------------------------------------------


@pytest.mark.asyncio
async def test_tab_cycles_four_panes(mock_store):
    """PANE-02/D-04: Tab visits all four panes in reading order TL->TR->BL->BR."""
    app = JoyApp()
    async with app.run_test() as pilot:
        await pilot.pause(0.2)
        await app.workers.wait_for_complete()
        # Initial focus should be on project list area
        focused = app.focused
        assert focused is not None

        # Collect pane IDs as we Tab through them
        pane_ids = []
        # Record starting pane
        pane_ids.append(_get_pane_id(app))

        for _ in range(3):
            await pilot.press("tab")
            await pilot.pause(0.05)
            pane_ids.append(_get_pane_id(app))

        # Should have visited 4 distinct panes
        assert len(set(pane_ids)) == 4, f"Expected 4 distinct panes, got: {pane_ids}"
        # Order must be: projects -> detail -> terminal -> worktrees
        expected_order = ["project-list", "project-detail", "terminal-pane", "worktrees-pane"]
        assert pane_ids == expected_order, f"Expected {expected_order}, got {pane_ids}"


@pytest.mark.asyncio
async def test_tab_wraps_around(mock_store):
    """PANE-02/D-05: Tab from last pane wraps to first pane."""
    app = JoyApp()
    async with app.run_test() as pilot:
        await pilot.pause(0.2)
        await app.workers.wait_for_complete()
        # Tab 4 times to cycle through all panes and wrap back
        for _ in range(4):
            await pilot.press("tab")
            await pilot.pause(0.05)
        # Should be back at the first pane (projects)
        pane_id = _get_pane_id(app)
        assert pane_id == "project-list", f"Expected wrap to project-list, got {pane_id}"


@pytest.mark.asyncio
async def test_shift_tab_reverses(mock_store):
    """PANE-02/D-04: Shift+Tab from projects pane goes to worktrees pane (reverse order)."""
    app = JoyApp()
    async with app.run_test() as pilot:
        await pilot.pause(0.2)
        await app.workers.wait_for_complete()
        # Focus should start on project list
        await pilot.press("shift+tab")
        await pilot.pause(0.05)
        pane_id = _get_pane_id(app)
        assert pane_id == "worktrees-pane", f"Expected worktrees-pane, got {pane_id}"


# ---------------------------------------------------------------------------
# D-13: sub_title updates per pane
# ---------------------------------------------------------------------------


@pytest.mark.asyncio
async def test_sub_title_updates_per_pane(mock_store):
    """D-13: sub_title shows the name of the focused pane."""
    app = JoyApp()
    async with app.run_test() as pilot:
        await pilot.pause(0.2)
        await app.workers.wait_for_complete()

        # Focus project list (initial)
        # sub_title starts as version but on_descendant_focus sets it on focus events
        # Force a focus event by tabbing away and back
        await pilot.press("tab")
        await pilot.pause(0.05)
        assert app.sub_title == "Detail"

        await pilot.press("tab")
        await pilot.pause(0.05)
        assert app.sub_title == "Terminal"

        await pilot.press("tab")
        await pilot.pause(0.05)
        assert app.sub_title == "Worktrees"

        await pilot.press("tab")
        await pilot.pause(0.05)
        assert app.sub_title == "Projects"


# ---------------------------------------------------------------------------
# Regression: existing v1.0 functionality still works
# ---------------------------------------------------------------------------


@pytest.mark.asyncio
async def test_existing_project_list_navigation(mock_store):
    """Regression: j/k and arrow key navigation in project list works in Grid layout."""
    app = JoyApp()
    async with app.run_test() as pilot:
        await pilot.pause(0.2)
        await app.workers.wait_for_complete()
        detail = app.query_one("#project-detail")
        # Initial project should be project-alpha
        assert detail._project is not None
        assert detail._project.name == "project-alpha"
        # Navigate down to project-beta
        await pilot.press("down")
        await pilot.pause(0.1)
        assert detail._project.name == "project-beta"


@pytest.mark.asyncio
async def test_existing_enter_and_escape(mock_store):
    """Regression: Enter focuses detail, Escape returns to list in Grid layout."""
    app = JoyApp()
    async with app.run_test() as pilot:
        await pilot.pause(0.2)
        await app.workers.wait_for_complete()
        # Enter to focus detail
        await pilot.press("enter")
        await pilot.pause(0.1)
        focused = app.focused
        assert focused is not None
        assert focused.id == "project-detail"
        # Escape to return to list
        await pilot.press("escape")
        await pilot.pause(0.1)
        focused = app.focused
        assert focused is not None
        assert focused.id in ("project-listview", "project-list")


# ---------------------------------------------------------------------------
# Helpers
# ---------------------------------------------------------------------------


def _get_pane_id(app: JoyApp) -> str:
    """Walk from focused widget up to find the pane container ID.

    Pane IDs: project-list, project-detail, terminal-pane, worktrees-pane.
    """
    pane_ids = {"project-list", "project-detail", "terminal-pane", "worktrees-pane"}
    node = app.focused
    while node is not None:
        if hasattr(node, "id") and node.id in pane_ids:
            return node.id
        node = node.parent
    # If focused widget itself is a pane
    if hasattr(app.focused, "id") and app.focused.id in pane_ids:
        return app.focused.id
    return f"unknown({app.focused})"
```

These tests will FAIL because app.py still uses Horizontal with only ProjectList and ProjectDetail. Plan 02 will refactor app.py to make them pass.
  </action>
  <verify>
    <automated>cd /Users/pieter/Github/joy && uv run pytest tests/test_pane_layout.py --co -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "test_four_panes_in_grid" tests/test_pane_layout.py` returns a match
    - `grep "test_tab_cycles_four_panes" tests/test_pane_layout.py` returns a match
    - `grep "test_tab_wraps_around" tests/test_pane_layout.py` returns a match
    - `grep "test_shift_tab_reverses" tests/test_pane_layout.py` returns a match
    - `grep "test_sub_title_updates_per_pane" tests/test_pane_layout.py` returns a match
    - `grep "test_existing_project_list_navigation" tests/test_pane_layout.py` returns a match
    - `grep "test_existing_enter_and_escape" tests/test_pane_layout.py` returns a match
    - `grep "PANE-01" tests/test_pane_layout.py` returns a match
    - `grep "PANE-02" tests/test_pane_layout.py` returns a match
    - `uv run pytest tests/test_pane_layout.py --co -q` collects 9 tests
    - `uv run pytest tests/test_pane_layout.py -x` FAILS (RED phase — tests describe behavior not yet implemented in app.py)
  </acceptance_criteria>
  <done>Nine tests exist in tests/test_pane_layout.py covering PANE-01 (4-pane grid), PANE-02 (Tab cycling), D-13 (sub_title), and regression (project list nav, Enter/Escape). Tests are collected by pytest. Tests FAIL because app.py has not been refactored yet (this is the RED phase).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates inert stub widgets with no user input or data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I (Info disclosure) | Stub pane "coming soon" | accept | Static placeholder text contains no sensitive info |
</threat_model>

<verification>
1. `uv run python -c "from joy.widgets import TerminalPane, WorktreePane"` exits 0
2. `uv run pytest tests/test_pane_layout.py --co -q` collects 9 tests
3. Tests FAIL (expected — RED phase), proving they test real behavior
4. Existing test suite still passes: `uv run pytest tests/ --ignore=tests/test_pane_layout.py -q` exits 0
</verification>

<success_criteria>
- TerminalPane and WorktreePane are importable from joy.widgets
- Both are focusable (can_focus=True), have border_title set, show "coming soon", have empty BINDINGS
- 9 failing tests describe the 4-pane layout contract
- Existing 188+ tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/08-4-pane-layout/08-01-SUMMARY.md`
</output>
