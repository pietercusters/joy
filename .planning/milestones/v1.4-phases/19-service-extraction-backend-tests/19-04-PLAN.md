---
phase: 19-service-extraction-backend-tests
plan: 04
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
  - 19-03
files_modified:
  - src/joy/app.py
  - tests/conftest.py
autonomous: true
requirements:
  - SRVC-04
  - SRVC-05
  - ARCH-02
  - ARCH-03
  - TEST-06

must_haves:
  truths:
    - "app.py is under 400 lines of code"
    - "app.py has under 10 instance variables in __init__"
    - "All @work(thread=True) decorators remain on app.py methods"
    - "All existing tests pass including test_sync.py and test_propagation.py (TEST-06)"
    - "Test isolation fixture accounts for any module path changes (ARCH-02)"
  artifacts:
    - path: "src/joy/app.py"
      provides: "Slim Textual composition root"
  key_links:
    - from: "src/joy/app.py"
      to: "src/joy/pane_coordinator.py"
      via: "self._coordinator"
      pattern: "_coordinator"
    - from: "src/joy/app.py"
      to: "src/joy/data_orchestrator.py"
      via: "self._orchestrator"
      pattern: "_orchestrator"
    - from: "src/joy/app.py"
      to: "src/joy/project_service.py"
      via: "self._project_svc"
      pattern: "_project_svc"
---

<objective>
Slim app.py to a composition root: verify it is under 400 LOC and under 10 instance variables, ensure all @work decorators remain, confirm test isolation fixture (ARCH-02), and validate all tests pass including the previously-noted tech debt (TEST-06).

Purpose: This is the verification and cleanup plan. After Plans 01-03 extract the three services, app.py should already be significantly slimmer. This plan audits the result, removes any dead code, consolidates remaining methods, and verifies all constraints from the ROADMAP success criteria.

Output: Final app.py under 400 LOC, all tests green, phase complete
</objective>

<execution_context>
@/Users/pieter/Github/joy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieter/Github/joy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/joy/app.py
@tests/conftest.py
@.planning/phases/19-service-extraction-backend-tests/19-01-PLAN.md
@.planning/phases/19-service-extraction-backend-tests/19-02-PLAN.md
@.planning/phases/19-service-extraction-backend-tests/19-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and slim app.py to composition root</name>
  <files>src/joy/app.py</files>
  <read_first>src/joy/app.py, src/joy/pane_coordinator.py, src/joy/data_orchestrator.py, src/joy/project_service.py</read_first>
  <action>
After Plans 01-03, audit app.py and apply final cleanup:

1. COUNT LINES: Run `wc -l src/joy/app.py`. Target: under 400.

2. COUNT INSTANCE VARIABLES: Count `self._` assignments in __init__. Target: under 10.
   Expected remaining instance vars after extraction:
   - self._config (Config)
   - self._repos (list[Repo])
   - self._coordinator (PaneCoordinator)
   - self._orchestrator (DataOrchestrator)
   - self._project_svc (ProjectService)
   - self._last_refresh_at (datetime | None)
   - self._refresh_failed (bool)
   - self._mr_fetch_failed (bool)
   - self._refresh_timer (object | None)
   - self._label_timer (object | None)
   - self._terminal_last_refresh_at (datetime | None)
   - self._terminal_refresh_failed (bool)
   - self._live_tab_ids (set[str])
   - self._tabs_creating (set[str])
   - self._sync_enabled (bool)

   That's 15 — still too many. Consolidate refresh-related state:

   a. Move refresh tracking into DataOrchestrator if not already done: _last_refresh_at, _refresh_failed, _mr_fetch_failed, _terminal_last_refresh_at, _terminal_refresh_failed can become properties on the orchestrator (add them in this task if needed). The orchestrator already tracks data state.

   b. _live_tab_ids is used only for tab-related checks — this should move into DataOrchestrator since it's set during _set_terminal_sessions.

   c. _tabs_creating is an in-flight guard for tab creation — keep on app.py (Textual-specific concurrency).

   After consolidation, expected remaining app.py instance vars:
   - self._config (or move to project_svc)
   - self._repos
   - self._coordinator
   - self._orchestrator
   - self._project_svc
   - self._refresh_timer
   - self._label_timer
   - self._tabs_creating
   - self._sync_enabled

   That's 9 — under the 10 target.

3. REMOVE DEAD CODE: Search for methods that are no longer called after extraction. Candidates:
   - _maybe_compute_relationships (if fully delegated to orchestrator in Plan 02)
   - _apply_worktree_link_status_fast (should already be deleted in Plan 02)
   - _propagate_mr_auto_add (if fully delegated in Plan 02)
   - _update_worktree_link_status (if inlined or delegated)

4. CONSOLIDATE REFRESH METHODS: The _mark_refresh_success, _mark_refresh_failure, _mark_terminal_refresh_success, _mark_terminal_refresh_failure, _update_refresh_label, _update_terminal_refresh_label, _update_all_refresh_labels, _format_age methods take ~55 lines. If line count is still over 400, move refresh tracking to DataOrchestrator as well:
   - DataOrchestrator gets mark_refresh_success(), mark_refresh_failure(), mark_terminal_refresh_success(), mark_terminal_refresh_failure(), format_age(), last_refresh_at, refresh_failed, etc.
   - _update_refresh_label and _update_terminal_refresh_label stay on app.py (they call query_one)
   - This should save ~20 lines from app.py

5. VERIFY @work DECORATORS: Grep for all @work(thread=True) in app.py and confirm they are still present. These methods MUST remain on app.py per SRVC-05:
   - _load_data
   - _load_worktrees
   - _load_terminal
   - _save_projects_bg
   - _do_create_tab_for_project
   - _do_activate_tab
   - _copy_value_bg
   - _do_open_global
   - _save_config_bg
   - _reload_repos
   - _open_defaults
   - _close_sessions_bg
   - _close_tab_bg
   - _append_to_archive_bg
   - _remove_from_archive_bg
   - _open_worktree_path

6. VERIFY SRVC-05: Confirm zero Textual imports in all three services:
   ```
   grep -c "from textual" src/joy/pane_coordinator.py  # must be 0
   grep -c "from textual" src/joy/data_orchestrator.py  # must be 0
   grep -c "from textual" src/joy/project_service.py   # must be 0
   ```

7. FINAL LINE COUNT: `wc -l src/joy/app.py` must be under 400.
  </action>
  <verify>
    <automated>wc -l src/joy/app.py | awk '{print ($1 < 400) ? "PASS: " $1 " lines" : "FAIL: " $1 " lines (must be < 400)"}'</automated>
  </verify>
  <acceptance_criteria>
    - wc -l src/joy/app.py shows under 400 lines
    - grep "self\._" src/joy/app.py | grep "__init__" context shows under 10 instance vars
    - grep -c "@work" src/joy/app.py returns 10+ (all @work decorators present)
    - grep -c "from textual" src/joy/pane_coordinator.py returns 0
    - grep -c "from textual" src/joy/data_orchestrator.py returns 0
    - grep -c "from textual" src/joy/project_service.py returns 0
  </acceptance_criteria>
  <done>app.py is under 400 LOC, under 10 instance variables, all @work decorators remain, all services have zero Textual imports</done>
</task>

<task type="auto">
  <name>Task 2: Verify test isolation fixture and all tests pass (ARCH-02, ARCH-03, TEST-06)</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py, src/joy/store.py</read_first>
  <action>
1. ARCH-02: Check if any new modules created in Plans 01-03 use path constants that need patching. The session-scoped fixture in conftest.py patches joy.store path constants (JOY_DIR, PROJECTS_PATH, CONFIG_PATH, REPOS_PATH, ARCHIVE_PATH). Since the three new services (pane_coordinator.py, data_orchestrator.py, project_service.py) import from joy.models and joy.resolver but NOT from joy.store directly (they are pure Python — store I/O stays in app.py's @work methods), the fixture should NOT need changes.

   VERIFY this assumption:
   ```
   grep "from joy.store" src/joy/pane_coordinator.py   # should be 0
   grep "from joy.store" src/joy/data_orchestrator.py   # should be 0
   grep "from joy.project_service" src/joy/project_service.py  # should be 0
   grep "joy.store" src/joy/project_service.py           # should be 0
   ```

   If any service DOES import from joy.store, update conftest.py to also patch the re-imported reference. For example, if project_service.py did `from joy.store import PROJECTS_PATH`, the fixture would need:
   ```python
   mp.setattr("joy.project_service.PROJECTS_PATH", tmp / "projects.toml")
   ```

2. TEST-06: Run test_sync.py and test_propagation.py specifically:
   ```
   uv run pytest tests/test_sync.py tests/test_propagation.py -x -v
   ```
   These should already pass (they passed before this phase). The requirement says "10 previously-failing tests" but STATE.md says these reference non-existent methods. Since all 21 tests in these files currently pass (verified pre-planning), TEST-06 is already satisfied. Confirm this is still true after all extractions.

   If any test references `JoyApp._propagate_mr_auto_add` directly (like test_propagation.py does via the _PropContext pattern), and that method's signature changed, update the test to call the DataOrchestrator's propagate_mr_auto_add instead.

3. Run the FULL test suite:
   ```
   uv run pytest -x --tb=short
   ```
   ALL tests must pass (ARCH-03).

4. If conftest.py needed no changes, document that in the summary (ARCH-02 satisfied — no new store imports in extracted services).
  </action>
  <verify>
    <automated>uv run pytest -x --tb=short 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_sync.py tests/test_propagation.py -x passes (TEST-06)
    - uv run pytest tests/test_pane_coordinator.py tests/test_data_orchestrator.py tests/test_project_service.py -x passes (all new tests)
    - uv run pytest -x passes (all tests, ARCH-03)
    - grep "from joy.store" src/joy/pane_coordinator.py src/joy/data_orchestrator.py src/joy/project_service.py returns 0 matches (ARCH-02 no fixture changes needed)
  </acceptance_criteria>
  <done>All tests pass, test isolation fixture verified, TEST-06 confirmed satisfied, ARCH-02 confirmed, ARCH-03 confirmed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification plan — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | T (Tampering) | test fixture | mitigate | Session-scoped monkeypatch ensures tests cannot read/write ~/.joy/ |
</threat_model>

<verification>
1. `wc -l src/joy/app.py` returns under 400 (SRVC-04)
2. `grep -c "self\._" src/joy/app.py` in __init__ block returns under 10 (SRVC-04)
3. `uv run pytest -x` passes all tests (ARCH-03)
4. `uv run pytest tests/test_sync.py tests/test_propagation.py -x` passes (TEST-06)
5. No new store imports in extracted services (ARCH-02)
6. All @work decorators remain on app.py (SRVC-05)
</verification>

<success_criteria>
app.py under 400 LOC, under 10 instance vars, all @work decorators present, zero Textual imports in services, all tests pass including sync/propagation, test isolation fixture correct.
</success_criteria>

<output>
After completion, create `.planning/phases/19-service-extraction-backend-tests/19-04-SUMMARY.md`
</output>
