---
phase: quick-260420-a6w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/joy/screens/preset_picker.py
  - src/joy/screens/repo_picker.py
  - tests/test_screens.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Opening 'Add Object' shows only a list with no text input box above it"
    - "Opening 'Assign Repo' shows only a list with no text input box above it"
    - "List has focus immediately on open — up/down/enter work without clicking"
    - "Escape still dismisses both modals"
    - "All filter-dependent tests are removed; remaining tests pass"
  artifacts:
    - path: src/joy/screens/preset_picker.py
      provides: "PresetPickerModal without Input widget or filter logic"
    - path: src/joy/screens/repo_picker.py
      provides: "RepoPickerModal without Input widget or filter logic"
    - path: tests/test_screens.py
      provides: "Test suite with filter tests removed, remaining tests updated"
  key_links:
    - from: PresetPickerModal.on_mount
      to: "#preset-list ListView"
      via: "listview.focus()"
      pattern: "focus.*preset-list"
    - from: RepoPickerModal.on_mount
      to: "#repo-list ListView"
      via: "listview.focus()"
      pattern: "focus.*repo-list"
---

<objective>
Remove the "Type to filter..." Input widget from PresetPickerModal (Add Object) and
RepoPickerModal (Assign Repo), along with all filter-related logic and tests.

Purpose: The textbox steals focus and creates a confusing UX — user opens the modal
expecting to navigate a list but has to Tab away from a filter input first.

Output: Both picker modals are plain list-only modals that focus the ListView on mount.
Filter-dependent tests removed; remaining modal tests pass.
</objective>

<execution_context>
@/Users/pieter/Github/joy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieter/Github/joy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Key facts from codebase inspection:
- `src/joy/screens/preset_picker.py` — PresetPickerModal
  - Input widget with id="filter-input", placeholder="Type to filter..."
  - `_filtered` state list
  - `on_input_changed`: rebuilds ListView based on filter query
  - `on_input_submitted`: auto-dismiss on 1 match or move focus to list
  - `on_key`: forwards up/down to ListView while filter has focus
  - `on_mount`: focuses the Input (wrong — should focus ListView)
  - `on_list_view_selected`: uses `self._filtered` to resolve selected item

- `src/joy/screens/repo_picker.py` — RepoPickerModal — same pattern

- `tests/test_screens.py` tests to DELETE:
  - `test_preset_picker_filter` (types "br" to filter, checks 1 item)
  - `test_repo_picker_select_none_returns_none` (types "none" to filter then Enter)
  - `test_repo_picker_filter_and_select` (types "alp" to filter then Enter)

- `tests/test_screens.py` tests to KEEP and VERIFY still pass:
  - `test_preset_picker_shows_all_presets` — checks 9 items in ListView (no filter used)
  - `test_preset_picker_escape_returns_none` — presses escape (no filter used)
  - `test_repo_picker_shows_repos_and_none_option` — checks 3 items (no filter used)
  - `test_repo_picker_escape_returns_cancelled` — presses escape (no filter used)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip filter Input from PresetPickerModal and RepoPickerModal</name>
  <files>src/joy/screens/preset_picker.py, src/joy/screens/repo_picker.py</files>
  <action>
In both files, make the following changes:

**preset_picker.py:**
1. Remove `Input` from the `from textual.widgets import` line (keep Label, ListItem, ListView, Static).
2. Remove `from textual.events import Key` import entirely.
3. Remove `self._filtered` from `__init__` — no longer needed.
4. In `compose`, remove the `yield Input(...)` line.
5. In `on_mount`, replace `self.query_one("#filter-input", Input).focus()` with `self.query_one("#preset-list", ListView).focus()`.
6. Delete `on_input_changed` method entirely.
7. Delete `on_key` method entirely.
8. Delete `on_input_submitted` method entirely.
9. In `on_list_view_selected`, replace `self._filtered[index]` with `self.ALL_PRESETS[index]` (direct index into the full static list, since there is no more filtering).

**repo_picker.py:**
1. Remove `Input` from the `from textual.widgets import` line (keep Label, ListItem, ListView, Static).
2. Remove `from textual.events import Key` import entirely.
3. Remove `self._filtered` from `__init__`.
4. In `compose`, remove the `yield Input(...)` line.
5. In `on_mount`, replace the Input focus with `self.query_one("#repo-list", ListView).focus()`.
6. Delete `on_input_changed` method entirely.
7. Delete `on_key` method entirely.
8. Delete `on_input_submitted` method entirely.
9. In `on_list_view_selected`, replace `self._filtered[index]` with `self._options[index]` (the full options list).

After edits, both files should have no reference to `filter`, `Input`, or `Key` events.
  </action>
  <verify>
    <automated>cd /Users/pieter/Github/joy && /Users/pieter/.nvm/versions/node/v22.17.1/bin/node -e "const {execSync}=require('child_process'); execSync('python -c \"from joy.screens.preset_picker import PresetPickerModal; from joy.screens.repo_picker import RepoPickerModal; print(\'imports ok\')\"', {cwd:process.cwd(), stdio:'inherit'})"</automated>
  </verify>
  <done>Both modal files import cleanly with no Input/filter references. No `Input` or `Key` or `_filtered` or `on_input_` or `on_key` symbols remain in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Remove filter tests; verify suite</name>
  <files>tests/test_screens.py</files>
  <action>
In `tests/test_screens.py`:

1. Delete the entire `test_preset_picker_filter` function (lines ~97-115).
2. Delete the entire `test_repo_picker_select_none_returns_none` function (lines ~396-412). This test types "none" to filter — no longer applicable.
3. Delete the entire `test_repo_picker_filter_and_select` function (lines ~415-430). This test types "alp" to filter — no longer applicable.

Do NOT modify any other tests. The four remaining picker tests (`test_preset_picker_shows_all_presets`, `test_preset_picker_escape_returns_none`, `test_repo_picker_shows_repos_and_none_option`, `test_repo_picker_escape_returns_cancelled`) require zero changes since they never interacted with the filter Input.

Also clean up any now-unused imports if `Input` was imported at the top of the test file solely for filter tests. Check line 6: `from textual.widgets import Button, Input, SelectionList, Static` — `Input` is likely still needed for other tests (NameInputModal, ValueInputModal), so leave the import unless inspection confirms it is unused.
  </action>
  <verify>
    <automated>cd /Users/pieter/Github/joy && python -m pytest tests/test_screens.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All remaining test_screens.py tests pass. The three filter tests are gone. No reference to "Type to filter" or "filter-input" remains in the test file for the picker modals.</done>
</task>

</tasks>

<verification>
cd /Users/pieter/Github/joy && python -m pytest tests/test_screens.py -x -q --tb=short
</verification>

<success_criteria>
- No "Type to filter..." Input widget in PresetPickerModal or RepoPickerModal
- ListView receives focus on mount in both modals
- `test_preset_picker_filter`, `test_repo_picker_select_none_returns_none`, `test_repo_picker_filter_and_select` are deleted
- All other test_screens.py tests pass
- `python -c "from joy.screens.preset_picker import PresetPickerModal"` succeeds cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260420-a6w-when-i-add-an-object-i-can-select-an-obj/260420-a6w-SUMMARY.md`
</output>
