---
phase: quick-260420-ku9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/joy/app.py
autonomous: true
requirements:
  - SYNC-SOURCE-PANE-BUG
must_haves:
  truths:
    - "Navigating to an unlinked worktree clears only TerminalPane, not WorktreePane"
    - "Navigating to an unlinked terminal session clears only WorktreePane, not TerminalPane"
    - "Source pane never loses its own selection as a result of its own sync reaction"
  artifacts:
    - path: src/joy/app.py
      provides: "Fixed _sync_from_worktree and _sync_from_session else branches"
  key_links:
    - from: "_sync_from_worktree else branch"
      to: "term_pane.clear_selection() only"
      via: "remove wt_pane.clear_selection()"
    - from: "_sync_from_session else branch"
      to: "wt_pane.clear_selection() only"
      via: "remove term_pane.clear_selection()"
---

<objective>
Remove the self-clearing calls from both sync else-branches so a source pane never
clears its own selection when the navigated item is unlinked.

Purpose: When a user navigates to a worktree or terminal that has no linked project,
the source pane must keep its current selection visible. Only the OTHER panes should
be cleared.

Output: Two lines removed from src/joy/app.py — one from each else branch.
</objective>

<execution_context>
@/Users/pieter/Github/joy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieter/Github/joy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pieter/Github/joy/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove source-pane self-clear from both sync else branches</name>
  <files>src/joy/app.py</files>
  <action>
    In `_sync_from_worktree` (around line 580), locate the else branch:

        else:
            # Worktree not linked to any project — clear both other panes
            wt_pane.clear_selection()
            term_pane.clear_selection()

    Remove `wt_pane.clear_selection()` (the source pane call). Keep only
    `term_pane.clear_selection()`. Update the comment to:
    `# Worktree not linked to any project — clear other panes`

    In `_sync_from_session` (around line 617), locate the else branch:

        else:
            # Session not linked to any project — clear both other panes
            term_pane.clear_selection()
            wt_pane.clear_selection()

    Remove `term_pane.clear_selection()` (the source pane call). Keep only
    `wt_pane.clear_selection()`. Update the comment to:
    `# Session not linked to any project — clear other panes`

    No other changes. Do not touch the if-branch logic (sync_to + matched guard),
    do not touch the docstrings, do not reformat surrounding code.
  </action>
  <verify>
    <automated>/Users/pieter/.nvm/versions/node/v22.17.1/bin/node -e "
const fs = require('fs');
const src = fs.readFileSync('src/joy/app.py', 'utf8');
const lines = src.split('\n');
// Find else branch in _sync_from_worktree (~580) — must NOT contain wt_pane.clear_selection after the else
const wtElseIdx = lines.findIndex((l, i) => i >= 578 && i <= 585 && l.includes('# Session') === false && l.trim() === 'else:' && lines[i-1].trim().startsWith('else:') === false);
// Simpler: just check the file does not have wt_pane.clear_selection inside _sync_from_worktree's else
const wtBlock = src.match(/_sync_from_worktree[\s\S]*?finally:/)?.[0] || '';
const termBlock = src.match(/_sync_from_session[\s\S]*?finally:/)?.[0] || '';
const wtElseHasSelfClear = wtBlock.match(/else:\s*\n.*#.*\n.*wt_pane\.clear_selection/);
const termElseHasSelfClear = termBlock.match(/else:\s*\n.*#.*\n.*term_pane\.clear_selection/);
if (wtElseHasSelfClear) { console.error('FAIL: wt_pane.clear_selection still in _sync_from_worktree else branch'); process.exit(1); }
if (termElseHasSelfClear) { console.error('FAIL: term_pane.clear_selection still in _sync_from_session else branch'); process.exit(1); }
console.log('PASS: neither source pane clears itself in its own sync else branch');
"</automated>
  </verify>
  <done>
    `_sync_from_worktree` else branch contains only `term_pane.clear_selection()`.
    `_sync_from_session` else branch contains only `wt_pane.clear_selection()`.
    All existing tests pass: `cd /Users/pieter/Github/joy && python -m pytest tests/ -x -q --ignore=tests/tui` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal event bus | Sync events are internal Textual messages — no external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ku9-01 | Tampering | _sync_from_worktree / _sync_from_session | accept | Pure internal state mutation; no external data involved. Risk: none. |
</threat_model>

<verification>
Run the fast test suite to confirm no regressions:

```bash
cd /Users/pieter/Github/joy && python -m pytest tests/ -x -q --ignore=tests/tui
```

Manually confirm: open joy, navigate to a worktree that has no linked project — the
worktree row should remain highlighted while TerminalPane clears. Navigate to a
terminal session with no linked project — the session row should remain highlighted
while WorktreePane clears.
</verification>

<success_criteria>
- Source pane keeps its cursor/selection when navigating to an unlinked item
- Opposing pane (and only the opposing pane) is cleared
- All fast unit tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260420-ku9-fix-sync-source-pane-clears-own-selectio/260420-ku9-01-SUMMARY.md`
</output>
