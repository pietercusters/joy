---
phase: quick
plan: 260423-kd7
type: execute
wave: 1
depends_on: []
files_modified:
  - src/joy/widgets/terminal_pane.py
  - src/joy/widgets/worktree_pane.py
  - src/joy/widgets/project_list.py
  - src/joy/widgets/project_detail.py
  - tests/test_cursor_restore.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Panes with clear_selection() active (cursor=-1, rows existed) preserve -1 after refresh"
    - "First-time population (cursor=-1, no prior rows) still auto-selects index 0"
    - "Identity-based cursor restore still works (cursor on item X stays on item X after rebuild)"
    - "Cursor clamping still works when selected item is removed"
  artifacts:
    - path: "src/joy/widgets/terminal_pane.py"
      provides: "Fixed cursor restore in set_sessions()"
      contains: "had_rows_before"
    - path: "src/joy/widgets/worktree_pane.py"
      provides: "Fixed cursor restore in set_worktrees()"
      contains: "had_rows_before"
    - path: "src/joy/widgets/project_list.py"
      provides: "Fixed cursor restore in _rebuild()"
      contains: "had_rows_before"
    - path: "src/joy/widgets/project_detail.py"
      provides: "Fixed cursor restore in _render_project()"
      contains: "had_rows_before"
    - path: "tests/test_cursor_restore.py"
      provides: "Tests for no-selection preservation"
  key_links:
    - from: "clear_selection()"
      to: "cursor restore logic"
      via: "had_rows_before flag distinguishes first-time from cleared-selection"
      pattern: "had_rows_before"
---

<objective>
Fix cursor restore logic in all four panes so that a deliberately cleared selection (cursor=-1
with rows present) is preserved across refresh/rebuild, instead of jumping to index 0.

Purpose: After clear_selection() is called (e.g., when selected project has no matching
terminals/worktrees), refreshing the pane should NOT auto-select the first item. The current
code conflates "first-time population" with "cleared selection" because both have
saved_identity=None and new_rows=True.

Output: Patched cursor restore in 4 widget files + regression tests.
</objective>

<execution_context>
@/Users/pieter/Github/joy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieter/Github/joy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/joy/widgets/terminal_pane.py
@src/joy/widgets/worktree_pane.py
@src/joy/widgets/project_list.py
@src/joy/widgets/project_detail.py
@tests/test_cursor_restore.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for no-selection preservation</name>
  <files>tests/test_cursor_restore.py</files>
  <behavior>
    - Test: ProjectList with cursor=-1 after clear (had rows, cursor set to -1) preserves -1 on set_projects refresh
    - Test: ProjectDetail with cursor=-1 after clear (had rows, cursor set to -1) preserves -1 on set_project refresh
    - Test: ProjectList first-time population (no prior rows) still starts at 0 (existing test, verify not broken)
    - Test: ProjectDetail first-time population still starts at 0 (existing test, verify not broken)
  </behavior>
  <action>
Add two new test functions to tests/test_cursor_restore.py:

1. `test_project_list_preserves_cleared_selection()`:
   - Create ProjectList, call set_projects with 3 projects, await pilot.pause
   - Assert cursor == 0 (first-time population works)
   - Set `plist._cursor = -1` then `plist._update_highlight()` (simulate clear_selection)
   - Call set_projects again with the SAME list (simulates refresh)
   - Assert cursor == -1 (no-selection preserved, NOT jumped to 0)

2. `test_project_detail_preserves_cleared_selection()`:
   - Create ProjectDetail, call set_project, await pilot.pause
   - Assert cursor == 0 (first-time)
   - Set `detail._cursor = -1` then `detail._update_highlight()` (simulate clear)
   - Call set_project again with same project
   - Assert cursor == -1

Run tests -- both new tests MUST FAIL (RED phase) since the fix is not yet applied.
Existing tests must still pass (the identity-based and first-time tests).
  </action>
  <verify>
    <automated>cd /Users/pieter/Github/joy && uv run pytest tests/test_cursor_restore.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>Two new tests exist and fail with "Expected cursor=-1, got 0" confirming the bug. All existing cursor restore tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix cursor restore logic in all four panes</name>
  <files>src/joy/widgets/terminal_pane.py, src/joy/widgets/worktree_pane.py, src/joy/widgets/project_list.py, src/joy/widgets/project_detail.py</files>
  <action>
Apply the same fix pattern to all four panes. In each pane's rebuild method, BEFORE the DOM
rebuild (after saving cursor state), add:

```python
had_rows_before = len(self._rows) > 0
```

Then replace the cursor restore block. The exact variable names differ per pane:

**terminal_pane.py** (lines ~334-346 in set_sessions):
- saved identity variable is `saved_name`
- Replace:
```python
if saved_name is not None and new_rows:
    for i, row in enumerate(new_rows):
        if row.session_name == saved_name:
            self._cursor = i
            break
    else:
        self._cursor = min(saved_index, len(new_rows) - 1)
elif new_rows:
    self._cursor = 0
else:
    self._cursor = -1
```
- With:
```python
if saved_name is not None and new_rows:
    for i, row in enumerate(new_rows):
        if row.session_name == saved_name:
            self._cursor = i
            break
    else:
        self._cursor = min(saved_index, len(new_rows) - 1)
elif new_rows and not had_rows_before and saved_index == -1:
    self._cursor = 0
elif new_rows and saved_index >= 0:
    self._cursor = min(saved_index, len(new_rows) - 1)
else:
    self._cursor = -1
```
- Keep the `self._update_highlight(emit=False)` call after.
- Place `had_rows_before = len(self._rows) > 0` right after `saved_index = self._cursor` (line ~257).

**worktree_pane.py** (lines ~368-380 in set_worktrees):
- saved identity variable is `saved_identity`
- Same pattern: replace `elif new_rows:` with the three-way branch.
- Keep `self._update_highlight(emit=False)` after.
- Place `had_rows_before = len(self._rows) > 0` right after `saved_index = self._cursor` (line ~314).

**project_list.py** (lines ~417-429 in _rebuild):
- saved identity variable is `saved_name`
- Same pattern: replace `elif new_rows:` with the three-way branch.
- Keep `self._update_highlight()` after.
- Place `had_rows_before = len(self._rows) > 0` right after `saved_index = self._cursor` (line ~376).

**project_detail.py** (lines ~212-226 in _render_project):
- Has `initial_cursor` branch FIRST (line 214) -- leave that completely unchanged.
- saved identity variable is `saved_identity`
- Replace `elif new_rows:` (line 223) with the three-way branch.
- Keep `self._update_highlight()` after.
- Place `had_rows_before = len(self._rows) > 0` right after `saved_index = self._cursor` (line ~176).

The three-way branch logic (same for all, just variable names differ):
1. Identity match found -> use matched index (existing, unchanged)
2. `new_rows and not had_rows_before and saved_index == -1` -> first-time population, set cursor=0
3. `new_rows and saved_index >= 0` -> had cursor but identity lost, clamp to valid range
4. else -> preserve -1 (cleared selection or empty)
  </action>
  <verify>
    <automated>cd /Users/pieter/Github/joy && uv run pytest tests/test_cursor_restore.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All tests in test_cursor_restore.py pass, including the two new no-selection preservation tests. The full test suite passes: `uv run pytest tests/ -x`.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_cursor_restore.py -v` -- all tests pass (identity restore, clamp, first-time, AND no-selection preservation)
2. `uv run pytest tests/ -x` -- full test suite passes (no regressions)
3. Manual smoke test: launch joy, select a project with no terminals, verify terminal pane stays unselected after refresh
</verification>

<success_criteria>
- All four panes preserve cursor=-1 when selection was deliberately cleared and refresh occurs
- All four panes still auto-select index 0 on first-time population (no prior rows)
- All existing cursor identity/clamp behavior unchanged
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/quick/260423-kd7-fix-preserve-no-selection-state-and-all-/260423-kd7-SUMMARY.md`
</output>
