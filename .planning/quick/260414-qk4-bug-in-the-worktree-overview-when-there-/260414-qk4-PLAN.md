---
phase: quick
plan: 260414-qk4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/joy/mr_status.py
  - tests/test_mr_status.py
autonomous: true
must_haves:
  truths:
    - "Pressing Enter on a worktree row with an MR opens the MR URL in a browser"
    - "Pressing Enter on a worktree row without an MR opens the worktree in the IDE"
    - "GitHub PRs populate the url field in MRInfo"
    - "GitLab MRs populate the url field in MRInfo"
  artifacts:
    - path: "src/joy/mr_status.py"
      provides: "MRInfo url field populated for both GitHub and GitLab"
    - path: "tests/test_mr_status.py"
      provides: "Tests verifying url is populated"
  key_links:
    - from: "src/joy/mr_status.py"
      to: "src/joy/widgets/worktree_pane.py"
      via: "MRInfo.url consumed by action_activate_row"
      pattern: "mr_info\\.url"
---

<objective>
Fix: Worktree Enter key opens MR URL when an MR exists, falls back to opening worktree in IDE.

Purpose: The worktree pane's `action_activate_row` already has the correct branching logic (check `mr_info.url`, open browser or IDE), but the `url` field in `MRInfo` is never populated by either the GitHub or GitLab fetch functions. Two small edits to `mr_status.py` fix this.

Output: `MRInfo.url` populated from `gh pr list --json url` (GitHub) and `mr.get("web_url")` (GitLab). Tests updated to verify.
</objective>

<execution_context>
@/Users/pieter/Github/joy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pieter/Github/joy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/joy/mr_status.py
@src/joy/models.py (line 163: url: str = "" already defined in MRInfo)
@src/joy/widgets/worktree_pane.py (lines 390-406: action_activate_row already checks mr_info.url)
@tests/test_mr_status.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add url field to test fixtures and assert url is populated</name>
  <files>tests/test_mr_status.py</files>
  <behavior>
    - Test: GitHub _fetch_github_mrs returns MRInfo with url="https://github.com/owner/repo/pull/42" for matching branch
    - Test: GitLab _fetch_gitlab_mrs returns MRInfo with url="https://gitlab.com/owner/repo/-/merge_requests/43" for matching branch
    - Test: Existing tests still pass (url field added to fixture JSON)
  </behavior>
  <action>
1. In `GITHUB_PR_JSON` fixture (line 17), add `"url": "https://github.com/owner/repo/pull/42"` to the PR dict.
2. In `GITHUB_PR_JSON_MULTI` fixture (line 32), add `"url": "https://github.com/owner/repo/pull/42"` to the first PR and `"url": "https://github.com/owner/repo/pull/99"` to the second.
3. In `GITLAB_MR_JSON` fixture (line 57), add `"web_url": "https://gitlab.com/owner/repo/-/merge_requests/43"` to the MR dict.
4. In `TestFetchGithubMrs.test_returns_mrinfo_for_matching_branches`, add assertion: `assert info.url == "https://github.com/owner/repo/pull/42"`.
5. In `TestFetchGitlabMrs.test_returns_mrinfo_for_matching_branches`, add assertion: `assert info.url == "https://gitlab.com/owner/repo/-/merge_requests/43"`.
6. In `TestFetchGithubMrs.test_empty_commits_list`, add `"url": "https://github.com/owner/repo/pull/10"` to the pr_data fixture and assert `info.url == "https://github.com/owner/repo/pull/10"`.
7. In `TestFetchGitlabMrs.test_calls_glab_ci_get_per_branch_with_mr`, add `"web_url"` fields to the mr_data dicts.

Run tests -- they MUST FAIL (url will be "" because mr_status.py doesn't pass it yet).
  </action>
  <verify>
    <automated>cd /Users/pieter/Github/joy && uv run pytest tests/test_mr_status.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Tests assert url is populated; tests fail because mr_status.py doesn't pass url yet (RED phase).</done>
</task>

<task type="auto">
  <name>Task 2: Populate MRInfo url in GitHub and GitLab fetch functions</name>
  <files>src/joy/mr_status.py</files>
  <action>
Two edits to `src/joy/mr_status.py`:

**GitHub fix** (`_fetch_github_mrs`):
1. Line 75: Add `url` to the `--json` fields string. Change:
   `"number,headRefName,isDraft,author,commits,statusCheckRollup"`
   to:
   `"number,headRefName,isDraft,author,commits,statusCheckRollup,url"`

2. Lines 96-103: Add `url=pr.get("url", "")` to the MRInfo constructor call.

**GitLab fix** (`_fetch_gitlab_mrs`):
3. Lines 145-152: Add `url=mr.get("web_url", "")` to the MRInfo constructor call.

That is the entire fix -- three small edits, no other changes needed. The `action_activate_row` in `worktree_pane.py` already has the correct logic to use `mr_info.url`.
  </action>
  <verify>
    <automated>cd /Users/pieter/Github/joy && uv run pytest tests/test_mr_status.py -x -q</automated>
  </verify>
  <done>All tests in test_mr_status.py pass. MRInfo.url is populated for both GitHub PRs and GitLab MRs. Enter key on worktree rows with MRs will now open the MR URL in a browser.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_mr_status.py -x -q` -- all tests pass
2. `uv run pytest tests/ -x -q --ignore=tests/tui` -- no regressions in fast test suite
</verification>

<success_criteria>
- MRInfo.url populated with full web URL for GitHub PRs (from `pr["url"]`)
- MRInfo.url populated with full web URL for GitLab MRs (from `mr["web_url"]`)
- All existing tests pass, new url assertions pass
- No changes needed to worktree_pane.py (logic already correct)
</success_criteria>

<output>
After completion, create `.planning/quick/260414-qk4-bug-in-the-worktree-overview-when-there-/260414-qk4-SUMMARY.md`
</output>
